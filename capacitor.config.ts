import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'composition-vs-inheritance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
